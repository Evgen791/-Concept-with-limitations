Этот код создаёт C++ понятие (concept), названное ComplexConcept, и проверяет, будет ли удовлетворять ему определённый класс.
ComplexConcept требует от класса наличия двух методов: hash, возвращающего значение, преобразуемое в long, и toString, возвращающего строку (std::string). 
Кроме того, класс должен не иметь виртуального деструктора.
В коде создаётся класс TestClass, который удовлетворяет требованиям ComplexConcept, и объявляется экземпляр этого класса в функции main(). 
Там же идёт проверка, удовлетворяет ли TestClass требованиям ComplexConcept, и, если это так, происходит вывод информации о функциях hash() и toString().
